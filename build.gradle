plugins {
  id "com.github.ben-manes.versions" version "0.54.0"
  id 'com.gradleup.shadow' version '9.4.1'
}

description = 'A template application for OpenGL (via JOGL) projects and experiments'

ext {
    javaLanguageVersion = 25
    lwjglVersion = '3.4.2'
    lwjglNatives = 'natives-linux'
    jomlVersion = '1.10.9'
}

allprojects {
  group = 'com.asteroid.duck'
  version = '0.0.1'
}


subprojects {
  apply plugin: 'java'

  java {
    toolchain {
      languageVersion.set(JavaLanguageVersion.of(javaLanguageVersion))
    }
  }

  tasks.withType(JavaCompile) {
    options.compilerArgs = ['-Xlint:unchecked', '-Xlint:deprecation']
  }

  test {
    jvmArgs = ['-Xshare:off']
    useJUnitPlatform()
  }
    repositories {
        mavenCentral()
    }

    dependencies {
        // Use a valid JUnit 5 release so Gradle's JUnit Platform can load the launcher and engine
        testImplementation 'org.junit.jupiter:junit-jupiter:6.1.2'
        testImplementation 'org.junit.jupiter:junit-jupiter-engine:6.1.2'
        // Ensure the JUnit Platform launcher is on the test runtime classpath for Gradle
        testImplementation 'org.junit.platform:junit-platform-launcher:6.1.2'
        testImplementation 'org.mockito:mockito-inline:5.2.0'
    }
}
